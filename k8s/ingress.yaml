apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: codebase-architect-ingress
  namespace: codebase-architect
  annotations:
    # For Azure Application Gateway
    appgw.ingress.kubernetes.io/backend-protocol: "http"
    
    # SSL/TLS
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    kubernetes.io/ingress.class: "traefik"
spec:
  tls:
  - hosts:
    - codebase-architect.local
    - api.codebase-architect.local
    secretName: codebase-architect-tls
  rules:
  - host: codebase-architect.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: codebase-architect-frontend
            port:
              number: 3000
  - host: api.codebase-architect.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: codebase-architect-backend
            port:
              number: 8000

