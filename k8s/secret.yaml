# DO NOT COMMIT REAL VALUES — replace with your actual credentials before deploying.
# This file contains PLACEHOLDER values safe for version control.
# Real secrets should be injected via CI/CD or a secrets manager (Azure Key Vault, etc.)
apiVersion: v1
kind: Secret
metadata:
  name: codebase-architect-secrets
  namespace: codebase-architect
type: Opaque
stringData:
  AZURE_OPENAI_ENDPOINT: "https://your-resource.openai.azure.com/"
  AZURE_OPENAI_API_KEY: "your-api-key-here"
